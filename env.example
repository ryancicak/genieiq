# Backend (required)
# Your Databricks workspace host, e.g. https://adb-1234567890123456.7.azuredatabricks.net
DATABRICKS_HOST=

# Local/dev token usage
# Set to "true" to allow `DATABRICKS_TOKEN` usage locally (Databricks Apps should prefer proxy/OAuth).
DATABRICKS_USE_ENV_TOKEN=true

# A Personal Access Token (PAT) for local development (do NOT commit real values)
DATABRICKS_TOKEN=

# Optional: Service principal OAuth (preferred for production/service-to-service)
DATABRICKS_CLIENT_ID=
DATABRICKS_CLIENT_SECRET=

# Admin controls
# Comma-separated list of admin emails (production defaults to non-admin unless listed)
ADMIN_EMAILS=

# Local/dev identity fallback (only used when NODE_ENV != production and proxy headers are missing)
DEV_USER_EMAIL=dev@example.com
DEV_USER_ID=dev-user
DEV_IS_ADMIN=false

# Lakebase (optional; if unset the app falls back to in-memory storage)
LAKEBASE_HOST=
LAKEBASE_PORT=5432
LAKEBASE_DATABASE=genieiq
#
# Local dev tip:
# Lakebase Postgres typically requires an OAuth token (NOT a PAT) for the password.
# Paste the 1-hour OAuth token from the Lakebase UI into:
LAKEBASE_TOKEN=

# Databricks Apps convenience (optional)
# Some deployments set these via `app.yaml` to avoid relying on implicit workspace context.
LAKEBASE_INSTANCE=
DATABRICKS_USER=

# Server
PORT=3001
