# GenieIQ Environment Configuration

# Server
PORT=3001
NODE_ENV=development

# Databricks Configuration
# When running locally, set these values
# When deployed as a Databricks App, authentication is handled automatically
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=your-personal-access-token

# Optional: Service Principal for Admin mode
# DATABRICKS_CLIENT_ID=your-service-principal-client-id
# DATABRICKS_CLIENT_SECRET=your-service-principal-secret

# Lakebase Provisioned Configuration
# Get these from your Lakebase instance in Databricks Compute
LAKEBASE_HOST=your-lakebase-instance.cloud.databricks.com
LAKEBASE_PORT=5432
LAKEBASE_DATABASE=genieiq
LAKEBASE_USER=your-username
LAKEBASE_PASSWORD=your-password

